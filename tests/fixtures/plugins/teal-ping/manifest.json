{
  "id": "teal-ping",
  "name": "Teal Ping",
  "version": "1.0.0",
  "apiVersion": 1,
  "engine": "^0.8.0",
  "description": "Fixture plugin: depends on acme-ping; dependency resolution must leave it enabled.",
  "server": "teal",
  "requires": ["acme-ping"],
  "tools": [
    {
      "toolId": "teal.ping",
      "server": "teal",
      "category": "custom",
      "description": "Ping a Teal endpoint.",
      "version": "1.0.0",
      "capabilities": ["teal", "ping"]
    }
  ]
}
